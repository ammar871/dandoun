import 'package:flutter/cupertino.dart';

class SetitingScreen extends StatelessWidget {
  const SetitingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}